/* usung-r29-color.js — 260812_홈페이지_수정_r2.pptx S07~S11 · S17 「색상·마감 이름 변경」
 *
 * 왜 필요한가
 *   usung-r8-data.js:187 colorOf(it) 는 it.finish 를 제일 먼저 돌려준다.
 *   그런데 finish 는 **도금 방식**(스텐도금·스텐도장·스파이얼)이지 색이 아니다.
 *   그래서 450Ø갓등 모달의 색상 칩 3개가 전부 도금방식으로 뜬다(S08 빨간박스).
 *   코브라 9종은 finish 가 비어 있어 태그→이름으로 흘러가 **제품명이 색상 칸에 그대로** 뜬다
 *   (S17 빨간박스 = 「색상·마감 | 90Ø롱망코브라220」).
 *
 * 왜 카탈로그 finish 를 안 고치나
 *   usung-r8-prod-a.js:33 이 기타옵션·디자인등에서 baseOf(it) = 「이름에서 finish 를 뺀 값」을
 *   그룹 키로 쓴다. finish 를 바꾸면 카드 묶음이 조용히 갈라진다 — r26 과 같은 뿌리다.
 *   colorOf 는 화면 표기 전용이고(칩 캡션·m-finnow·속성 pill·기본규격 색상·마감 행)
 *   어디서도 조회 키로 쓰이지 않는다. 그래서 **여기만** 갈아끼운다.
 *
 * 키는 it.stem — usung-catalog-data.js 각 행의 0번 필드다(usung-r8-prod-a.js:8 에서 매핑).
 *
 * 되돌리기: 이 파일 삭제 + api/inject.js 의 링크 1줄 삭제.
 */
(function () {
  'use strict';

  var w = window;

  /* ── LED조명 ────────────────────────────────────────────────────────────
     덱 본문이 「왼쪽에서부터 순서대로」라고 적어준 순서 그대로다.
     좌→우 순서 = UP_DATA 행 순서와 일치하는 것을 카탈로그 이름 끝 토큰으로 교차확인했다
     (예: led006 「450갓등 스텐도금 동도금」 → 덱 1번째 = 동도금). */
  var LBL = {
    /* S07 350Ø갓등(슬림) — 「스텐도금이 아니라 은색은 크롬, 동색은 동도금으로」 */
    led004: '크롬',                 // 350갓등 슬림 스텐도금 크롬
    led005: '동도금',               // 350갓등 얕은갓 스텐도금 동도금

    /* S08 450Ø갓등 — 동도금 / 실버함마 / 검정함마, 스파이얼 */
    led006: '동도금',               // 450갓등 스텐도금 동도금
    led007: '실버함마',             // 450갓등 스텐도장 실버함마
    led008: '검정함마, 스파이얼',   // 450갓등 스파이얼 검정함마

    /* S09 500Ø갓등(카탈로그 grp 는 '500Ø항아리등' · 표기는 r26 이 바꾼다)
       — 동함마, 스파이얼 / 빨강함마.
       led009 의 finish 는 스텐도장인데 덱은 스파이얼을 함께 적었다. 모순이 아니다 —
       usung-r19-parts-data.js:FINISH 가 「도장 = 스텐도장 · 스파이얼」로 같은 계열로 묶는다. */
    led009: '동함마, 스파이얼',     // 500Ø항아리등 스텐도장 동함마
    led010: '빨강함마',             // 500Ø항아리등 스텐도장 빨강함마

    /* S10 600Ø우주선 — 신주도금 / 검정함마, 스파이얼 */
    led013: '신주도금',             // 600Ø우주선등 스텐도금 신주도금
    led014: '검정함마, 스파이얼',   // 600Ø우주선등 스파이얼 검정함마

    /* S11 450Ø우주선 — 동도금 / 신주도금 */
    led011: '동도금',               // 450Ø우주선등 스텐도금 동도금
    led012: '신주도금',             // 450Ø우주선등 스텐도금 신주도금

    /* ★ 350Ø갓등(반달) 3종은 덱에 슬라이드가 없다 — 아래는 추론이다.
       S07~S11 의 확정 11건이 전부 「이름의 마지막 토큰 = 색상」이라 같은 규칙을 적용했다.
       빼려면 이 3줄만 지우면 된다(그러면 반달만 계속 도금방식으로 뜬다). */
    led001: '동도금',               // 350갓등 반달 스텐도금 동도금
    led002: '검정함마',             // 350갓등 반달 스텐도장 검정함마
    led003: '동함마',               // 350갓등 반달 스파이얼 동함마

    /* ── S17 코브라(하향식후드) 9종 ──────────────────────────────────────
       덱의 3×3 표를 화면의 3×3 타일 격자에 그대로 얹은 것이다(행 우선).
       표 색과 타일 사진 색이 9/9 일치하는 것을 눈으로 대조했다.
       ★ usung-r8-prod-a.js:11-18 이 이름 기준 중복 제거를 하므로 실제로 화면에 남는 건
         먼저 나오는 cobrat00N 쪽이다. cobra00N 은 보험으로 같이 걸어 둔다. */
    cobrat001: '크롬',                                cobra001: '크롬',                                // 90Ø롱망코브라220
    cobrat002: '신주도금',                            cobra002: '신주도금',                            // 75Ø망대코브라200
    cobrat003: '헤어라인',                            cobra003: '헤어라인',                            // 사각코브라160
    cobrat004: '실버함마',                            cobra004: '실버함마',                            // 75Ø주물코브라200Ø갓160
    cobrat005: '동함마(주물)',                        cobra005: '동함마(주물)',                        // 75Ø주물나팔코브라100
    cobrat006: '동함마(철-스텐으로도 제작 가능-)',    cobra006: '동함마(철-스텐으로도 제작 가능-)',    // 90Ø 각코브라
    cobrat007: '동도금',                              cobra007: '동도금',                              // 75Ø코브라270
    cobrat008: '동도금',                              cobra008: '동도금',                              // 75Ø코브라170(2단캡)
    cobrat009: '헤어라인',                            cobra009: '헤어라인'                             // 75Ø코브라170
  };

  /* colorOf 는 usung-r8-data.js 최상위의 함수 선언이라 = window 속성이다.
     usung-r8-prod-b.js 는 맨이름 colorOf(...) 로 부르므로 스코프체인이 여기로 온다.
     (r23 이 renderPartsPage 에 쓴 것과 같은 수법) */
  function wrap() {
    if (w.__r29color) return true;
    if (typeof w.colorOf !== 'function') return false;
    var orig = w.colorOf;
    w.colorOf = function (it) {
      if (it && LBL.hasOwnProperty(it.stem)) return LBL[it.stem];
      return orig.apply(this, arguments);
    };
    w.__r29color = 1;
    return true;
  }

  /* defer 순서상 r8-data 는 이미 실행됐지만, 늦게 붙는 경우를 위한 짧은 재시도. */
  if (!wrap()) {
    var n = 0;
    var iv = setInterval(function () {
      if (wrap() || ++n > 80) clearInterval(iv);
    }, 100);
  }
})();

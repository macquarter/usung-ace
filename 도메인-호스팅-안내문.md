# 유성에이스 도메인 연결 안내문

## 현재 상태

현재 새 홈페이지는 Vercel에 배포되어 아래 주소에서 운영 중입니다.

- 메인: `https://usung-ace.vercel.app`
- 관리자: `https://usung-ace.vercel.app/admin`

유성에이스의 기존 도메인(예: usungace.com 또는 usungace.co.kr)을 이 새 홈페이지에 연결하려면 아래 자료가 필요합니다.

---

## 유성에이스에 요청할 자료

### 1. 현재 도메인 정보

유성에이스에 다음을 확인해 주세요:

- **현재 사용 중인 도메인 주소** (예: usungace.com, usungace.co.kr 등)
- **도메인 등록 업체(레지스트라)** — 어디서 도메인을 구매/관리하고 있는지
  - 예: 가비아(gabia), 카페24, 후이즈, 닷네임코리아, GoDaddy 등
- **도메인 관리 페이지 로그인 정보** (아이디/비밀번호)
  - 또는 DNS 설정을 직접 변경할 수 있는 담당자 연락처

### 2. 현재 호스팅 정보

- **현재 홈페이지 호스팅 업체** (예: 카페24, 가비아, AWS 등)
- **기존 홈페이지를 계속 유지할지, 완전히 교체할지** 여부

---

## 도메인 연결 방법 (2가지)

### 방법 A: Vercel에 커스텀 도메인 연결 (권장)

가장 깔끔한 방법입니다. 도메인 자체를 Vercel 서버로 연결합니다.

**진행 순서:**

1. 유성에이스 도메인 관리 페이지에 로그인
2. DNS 설정에서 다음 레코드 추가/변경:
   - `A 레코드`: `76.76.21.21` (Vercel IP)
   - `CNAME 레코드` (www): `cname.vercel-dns.com`
3. Vercel 프로젝트 설정에서 커스텀 도메인 추가
4. SSL 인증서 자동 발급 (Vercel이 자동 처리)

**장점:** 속도 빠름, SSL 자동, 관리 편리
**소요 시간:** DNS 전파 후 최대 24~48시간

### 방법 B: 기존 호스팅에서 리다이렉트(포워딩)

기존 호스팅은 유지하면서, 방문자를 자동으로 새 주소로 보내는 방법입니다.

**진행 순서:**

1. 기존 호스팅 관리 페이지에서 "URL 포워딩" 또는 "리다이렉트" 설정
2. 대상 URL: `https://usung-ace.vercel.app`
3. 301 리다이렉트(영구 이동) 선택

**장점:** 기존 설정 크게 변경 안 해도 됨
**단점:** 주소창에 Vercel 주소가 노출될 수 있음, 속도 약간 느림

---

## 유성에이스에 보낼 메시지 예시

> 안녕하세요, 유성에이스 홈페이지 리뉴얼 관련하여 도메인 연결을 위해 아래 정보가 필요합니다.
>
> 1. 현재 사용 중인 도메인 주소 (예: usungace.com)
> 2. 도메인을 어디서 관리하고 계신지 (가비아, 카페24, 후이즈 등)
> 3. 도메인 관리 페이지 로그인 정보 (또는 DNS 설정 가능한 담당자)
> 4. 현재 호스팅 업체명
> 5. 기존 홈페이지를 완전히 교체할 것인지, 병행 운영할 것인지
>
> 위 정보를 주시면 새 홈페이지에 기존 도메인을 연결하는 작업을 진행하겠습니다.
> 작업 완료 후에는 usungace.com 으로 접속하면 바로 새 홈페이지가 뜹니다.

---

## 참고: Vercel 커스텀 도메인 설정 절차 (우리가 할 일)

유성에이스로부터 자료를 받은 후:

1. Vercel 대시보드 → 프로젝트(usung-ace) → Settings → Domains
2. 도메인 입력 (예: usungace.com) → Add
3. Vercel이 안내하는 DNS 레코드를 도메인 관리 페이지에 입력
4. 검증 완료 후 SSL 자동 발급
5. www 리다이렉트도 자동 설정됨

전체 작업 시간: 약 30분~1시간 (DNS 전파 대기 제외)
